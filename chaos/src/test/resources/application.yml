chaos:
  config:
    provider:
      type: jdbc
      namespace: com.ddm
      group: cfd
      tags: [ "gray", "hotfix" ]
      config:
        url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
        username: sa
        password: ""
        init_sql: "true"
    ttl: 30S